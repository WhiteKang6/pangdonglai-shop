<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.database.homework.pangdonglaishop.mapper.OrderItemMapper">
    <insert id="insertBatch">
        insert into order_item (order_id, product_id, count, price, create_time)
        values
        <foreach collection="orderItems" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.count}, #{item.price}, getdate())
        </foreach>
    </insert>
</mapper>